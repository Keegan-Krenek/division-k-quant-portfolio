NOTE: The Proto‑Database is currently in active development, with several layers fully implemented and others in progress. It is designed for continuous expansion as Division‑K research evolves.


SQL INSERT templates

Migration scripts

ERD refinements

JSON schema shadow layer

Query packs (timing sync, actor sync, spike clustering)

Dashboard design

Schema linking system

Engine instability detection

Actor interference modeling

README.md for the Proto-database folder

build.sh (bash script to auto-create DB + run schema)

drop_all.sql (clean wipe + rebuild)

insert_templates.sql for each table

example_queries.sql (joins, structural queries, etc.)